import 'package:flutter/material.dart';
import 'package:ib_demo/widgets/lists/my_movies_list.dart';

class MyMoviesTab extends StatelessWidget {
  const MyMoviesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const MyMoviesList();
  }
}
